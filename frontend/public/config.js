window.QUANTHUNT_CONFIG = Object.freeze({
	// Leave empty on Vercel so /api/* goes through the Vercel rewrite.
	// For local browser-only testing, app.jsx falls back to http://127.0.0.1:8000.
	API_BASE: "",
});
